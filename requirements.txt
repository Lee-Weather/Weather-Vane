# ============================================================
# AI 论文每日推送系统 — Python 依赖清单
# 环境：conda ai_base
# 安装：pip install -r requirements.txt
# ============================================================

# ── Skill 1: fetcher ────────────────────────────────────────
httpx>=0.27.0           # 异步 HTTP 客户端（arXiv / HF / PWC / S2 请求）
feedparser>=6.0.11      # arXiv Atom XML 解析
python-dateutil>=2.9    # 日期解析与时区转换

# ── Skill 4: storage ────────────────────────────────────────
# SQLite3 为 Python 内置模块，无需额外安装

# ── Skill 5: notifier ───────────────────────────────────────
python-telegram-bot>=21.0   # Telegram Bot 推送（可选）
# smtplib 为 Python 内置模块，无需额外安装

# ── 通用工具 ────────────────────────────────────────────────
python-dotenv>=1.0.0    # 从 .env 文件加载环境变量

# ── 测试工具 ────────────────────────────────────────────────
pytest>=8.0.0           # 单元测试框架
respx>=0.21.0           # httpx 网络请求 Mock（测试用）
pytest-asyncio>=0.23.0  # 支持 async 测试函数
